import { Map } from "../models";

interface MapSelectorProps {
  onMapSelection: (selectedMap: Map) => void;
}

export const MapSelector: React.FC<MapSelectorProps> = ({ onMapSelection }) => {
  return (

  )
}
